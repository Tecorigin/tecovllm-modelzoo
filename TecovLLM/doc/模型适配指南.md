# 模型推理适配指南

本文档介绍如何将推理模型适配至太初 AI 加速卡（SDAA 平台），使其能够基于 TecovLLM 框架正常运行。整体流程如下：

| # | 阶段 | 说明 |
|---|------|------|
| 1 | 检查推理环境 | 确认开发环境（硬件 + 容器）满足任务需求 |
| 2 | 模型适配 | 对模型源码和推理接口进行适配，使其能在 TecovLLM 框架中运行 |
| 3 | 自定义算子开发与接入 | 为 SDAA 暂不支持或精度不达标的算子开发适配实现 |
| 4 | 精度分析 | 验证推理精度是否达标，若不达标则进行调试 |
| 5 | 性能分析 | 测试吞吐、延迟等关键指标，若不达标则进行调优 |
| 6 | 添加 README | 为适配的模型编写推理使用说明 |
| 7 | 提交 PR | 完成所有测试后，将代码提交至仓库 |

---

## 1. 检查推理环境

开始适配前，建议先熟悉开发环境的软硬件配置，确保容器、框架、加速卡等资源就绪，以便适配和推理过程顺利进行。

### 1.1 检查硬件信息

使用 `teco-smi` 命令查看太初加速卡的设备列表与工作状态：

```bash
teco-smi
```

输出示例：

```
+-----------------------------------------------------------------------------+
|  TECO-SMI: 1.15.0        SDAADriver: 3.1.0        SDAARuntime: 3.1.0        |
|-------------------------------+----------------------+----------------------|
| Index  Name                   | Bus-Id               | Health      SPE-Util |
|        Temp          Pwr Usage|          Memory-Usage|                      |
|=============================================================================|
|   0    TECO_AICARD_01         | 00000002:05:00.0     | OK                0% |
|        42C                98W |        0MB / 65536MB |                      |
|-------------------------------+----------------------+----------------------|
|   1    TECO_AICARD_01         | 00000002:06:00.0     | OK                0% |
|        40C                92W |        0MB / 65536MB |                      |
+-------------------------------+----------------------+----------------------+
```

重点关注以下字段：

| 字段 | 说明 | 正常状态 | 异常处理 |
|------|------|----------|----------|
| **Memory-Usage** | 设备内存使用状态（已用 / 总量） | 已用内存远小于总量 | 内存占用异常时检查残留进程 |
| **Health** | 设备健康状态 | `OK` | 出现 `DEVICE_LOST`、`HEARTBEAT_ERROR` 等异常时，请联系太初技术支持 |
| **SPE-Util** | SPE 计算核心使用率 | 正常数值 | 显示 `N/A` 表示芯片掉卡，请联系太初技术支持 |

### 1.2 检查容器软件信息

TecovLLM-ModelZoo 以 Docker 容器方式提供服务，容器内已预装所有基础软件及 TecovLLM 推理框架。进入容器后，按以下步骤确认软件版本。

**步骤 1：查看 vLLM / TecovLLM 版本**

```bash
pip list | grep vllm
```

示例如下：

```
tecovllm            3.2.0
vllm                0.17.2.dev0+g95c0f928c.d20260511.empty
```

**步骤 2：查看 Torch-SDAA 及依赖组件版本**

```bash
vllm
```

若终端成功输出 Torch-SDAA 及各依赖组件的版本信息，表明运行环境就绪。示例如下：

```
--------------+-----------------------------------------------------------------------------------
Host IP      | 10.71.12.30
PyTorch      | 2.7.1
Torch-SDAA   | 3.1.1a2+gitd0e5a98
--------------+-----------------------------------------------------------------------------------
SDAA Driver  | 3.1.0 (N/A)
SDAA Runtime | 3.1.0 (/opt/tecoai/lib64/libsdaart.so)
SDPTI        | 1.7.0 (/opt/tecoai/lib64/libsdpti.so)
TecoDNN      | 3.1.0 (/opt/tecoai/lib64/libtecodnn.so)
TecoBLAS     | 2.2.1 (.../libtecoblas.so)
CustomDNN    | 3.1.0a0 (/opt/tecoai/lib64/libtecocustom.so)
TecoRAND     | 3.1.0 (/opt/tecoai/lib64/libtecorand.so)
TCCL         | 3.1.0 (/opt/tecoai/lib64/libtccl.so)
--------------+-----------------------------------------------------------------------------------
```

---

## 2. 模型适配

### 2.1 模型分析

适配前请完成以下确认，以保障后续流程顺利开展：

1. **模型可运行验证**：确认模型在 CPU 或 GPU 环境下能正常加载并完成基础推理，无报错或异常退出。
2. **量化格式确认**：当前仅支持 **FP16** 和 **INT8** 两种量化格式，其他格式暂不兼容。
3. **并行策略确认**：
   - **TP（张量并行）**：按硬件核组数配置，单卡为 4 核组，单卡运行时 TP = 4。
   - **PP / DP / EP**：并行规则与标准 CUDA 保持一致。

### 2.2 模型组网

使用 `torch` 和 `torch_sdaa` 提供的算子与函数实现模型的网络结构。具体实现方式请参考 vLLM 官网的 [OPT 模型适配示例](https://docs.vllm.ai/en/stable/contributing/model/basic/)。

本文使用的 Seed-OSS 模型网络结构实现代码，见 [seed_oss.py](../models/Seed-OSS-36B-Instruct/seed_oss.py)。

### 2.3 模型注册

将模型注册到 TecovLLM 框架，使其可被推理引擎识别和调用。

**步骤 1：定位 TecovLLM 安装路径**

```bash
pip show vllm
```

返回结果中的 `Location` 字段即为包根目录，记为 `{vllm-root}`。

**步骤 2：放置模型文件**

将模型组网文件放到 `{vllm-root}/vllm_sdaa/models/` 目录下。

**步骤 3：注册模型**

在 `{vllm-root}/vllm_sdaa/models/__init__.py` 文件中添加注册代码：

```python
def register_model():
    from vllm import ModelRegistry
    ModelRegistry.register_model(
        "SeedOssForCausalLM", "vllm_sdaa.models.seed_oss:SeedOssForCausalLM"
    )
```

> **说明**：如需新增更多模型，继续在 `register_model` 函数中添加 `ModelRegistry.register_model` 语句即可。

### 2.4 运行模型

#### 2.4.1 离线推理

参考 [离线推理文档](http://docs.tecorigin.com/release/teco_vllm/v3.1.0/#d6692347dc1c533ab3651f83213ae646)，完成模型离线推理验证。

#### 2.4.2 在线推理

参考 [在线推理文档](http://docs.tecorigin.com/release/teco_vllm/v3.1.0/#43351f4b9e305913b221a171b0a5a59d)，完成模型在线推理验证。

---

## 3. 自定义算子开发与接入

在模型迁移过程中，开发者通常需要在以下场景中开发自定义算子：
- SDAA 硬件暂不支持该算子
- 需要对特定算子进行**性能优化**（如算子融合、优化特定 shape 下的算子性能等）

此时可通过定制开发适配算子并将其接入 TecovLLM 推理框架来解决。太初官方在 [算子开发仓库](https://github.com/Tecorigin/teco-ops) 中提供了部分基础版算子供开发者作为参考示例。算子开发及 PyTorch 框架接入方法，请参考该仓库中的相关文档。

---

## 4. 精度分析

模型适配完成后，需通过精度验证确认推理结果正确性。具体流程如下：

### 4.1 基准精度（CUDA 环境）

1. **环境要求**：vLLM 版本为 **0.20.2**，Evalscope 版本为 **1.8.1**。
2. **配置要求**：使用 vLLM **默认配置**运行，不启用任何优化策略（如量化、并行、PD 分离等）。
3. **数据集要求**：使用 Evalscope 在标准数据集上跑一版结果作为精度基准：
   - **通用语言模型**：使用标准通用数据集
   - **多模态模型**：使用对应的多模态数据集
   - **特定领域模型**：使用对应领域的数据集（如 OCR 模型使用 OCR 数据集，语音模型使用语音数据集）

### 4.2 目标精度（SDAA 环境）

在 SDAA 环境上运行 **相同的数据集与配置**，得到目标推理结果。

### 4.3 精度判定标准

将 SDAA 推理结果与 CUDA 基准结果进行逐项对比，**误差范围在 ±2% 以内** 视为精度达标。

### 4.4 精度分析工具

若精度不达标，可使用精度分析工具定位差异来源：

| 工具 | 说明 | 参考文档 |
|------|------|----------|
| **精度分析工具** | 当模型精度异常时，使用该工具逐层对齐 CUDA 与 SDAA 的推理精度，定位精度异常的层，帮助加快精度排查进度 | [精度分析工具使用指南](http://docs.tecorigin.net/release/teco_precision/) |
| **Evalscope** | 评测工具，用于在多任务场景下验证模型整体精度表现 | [Evalscope 模型评测指南](https://evalscope.readthedocs.io/zh-cn/latest/get_started/supported_dataset/index.html) |

---

## 5. 性能分析

为确保模型在 SDAA 平台上的运行效率，需完成以下测试与调优工作：

| 步骤 | 说明 | 参考文档 |
|------|------|----------|
| **性能测试** | 使用 EvalScope 性能测试工具，对吞吐量、延迟、显存占用等关键指标进行基准测试 | [EvalScope 性能测试工具](http://docs.tecorigin.com/release/teco_vllm/v3.1.0/#e468da21ea2151fdba18302f31212be7) |
| **性能分析** | 使用性能分析工具，拆解算子耗时与通信开销，定位性能瓶颈 | [性能分析工具使用说明](http://docs.tecorigin.net/release/teco_vllm/#98a6915b6e2b5e5a8387632edde8070b) |

---

## 6. 添加 README

为适配的模型编写推理使用说明，方便其他开发者快速复用。文档格式请参考已适配模型 [Seed-OSS-36B-Instruct README](../models/Seed-OSS-36B-Instruct/README.md)，各章节需严格对齐，必须包含以下内容：

```markdown
# 模型名称

## 1. 模型概述
对模型进行简介。

---

## 2. 快速开始

### 2.1 模型权重下载
模型权重下载命令（使用代码块标注语言）。

### 2.2 模型注册
分步骤说明模型注册流程，包括：
- 安装 TecovLLM 和 vLLM 安装包
- 定位 TecovLLM 安装路径（`pip show vllm`）
- 放置模型组网文件
- 在 `__init__.py` 中注册模型（给出代码示例）

### 2.3 启动推理服务
vLLM 服务启动命令（使用代码块标注语言）。

### 2.4 精度验证

#### 2.4.1 运行精度测试
Evalscope 数据集精度验证命令。

#### 2.4.2 精度结果
以表格形式展示精度结果，需包含 **CUDA（基准）** 和 **SDAA（太初）** 两行数据，并注明精度误差判定标准（±2% 以内达标）。

### 2.5 性能验证

#### 2.5.1 运行性能测试
Evalscope 性能测试命令。

#### 2.5.2 性能结果
以代码块展示 Benchmark 性能数据。
```

---

## 7. 提交 PR

完成所有测试并通过后，将代码提交到 TecovLLM-ModelZoo 仓库。PR 提交流程请参考 [PR 提交规范](./pr提交规范.md)。
